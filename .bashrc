# ~/.bashrc

# set simple blue prompt
export PS1="\e[0;34m\W \$ \e[m"

alias ra="ranger"
alias gs="git status"
alias gc="git commit -m 'commit'"
alias ls='ls -lhN --color=auto --group-directories-first'
alias grep="grep --color=auto"
alias ccat="highlight --out-format=ansi"

